export class System {
    constructor(ip, port, username, password, mail, alert) {
        this.ip = ip;
        this.port = port;
        this.username = username;
        this.password = password;
        this.mail = mail;
        this.alert = alert;
    }
}